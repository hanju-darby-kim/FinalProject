<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="manager">

	<select id="check" parameterType="Integer" resultType="Integer">
		select count(*) from stubycur a
    	inner join curopen b on a.curopenseq = b.seq
    	inner join manager c on b.managerseq = c.seq
    	inner join member d on a.seq = d.seq
    	inner join tblattendance e on b.seq = e.sseq where c.seq = #{seq} and e.day = sysdate;
	</select>
	
</mapper>