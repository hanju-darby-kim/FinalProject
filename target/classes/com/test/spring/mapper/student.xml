<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="student">

	<select id="loginCheck" parameterType="com.test.spring.dto.StudentDTO">
		select count(*) from tblAttendance where date .....
	</select>
	
	<insert id="checkIn">
		
	</insert>
	
	<update id="checkOut">
		
	</update>
	
</mapper>